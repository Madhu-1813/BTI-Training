
Take the java application with postgres 

change the port from 8080 to any other available port 

instead of the app container is build on the fly when you run the compose file, build an image and push the image to the container registry

use that image in app container.. 

Perform all crud operations using postman, possible create a separate workspace, and a collection along with postman environment variables 

after performing crud operations check the data by accessing Adminer container.

